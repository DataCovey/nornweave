"""Thread grouping logic (References, In-Reply-To, subject). Placeholder."""


def resolve_thread_id(
    message_id: str | None,
    references: str | None,
    in_reply_to: str | None,
    subject: str,
) -> str | None:
    """Resolve or create thread id from headers. Placeholder."""
    return None
