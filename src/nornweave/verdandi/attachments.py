"""Attachment text extraction (PDF, CSV). Phase 2. Placeholder."""


def extract_text_from_attachment(
    _content_bytes: bytes,
    _content_type: str,
    _filename: str | None = None,
) -> str:
    """Extract plain text from attachment. Placeholder."""
    return ""
